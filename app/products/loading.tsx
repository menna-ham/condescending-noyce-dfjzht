export default function Loading() {
  // display loading state during navigating to new route
  // allows shared layout to stay interactive while new route segment loads
  return (
    <>
      <h1> Loading</h1>
    </>
  );
}
